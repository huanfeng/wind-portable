# 安全策略

## 为什么这个项目需要单独的安全策略

wind_portable 是便携启动器：它注册/注销 TSF、启停输入法服务、写开机自启项，并会
**通过 UAC 提权自我拉起**执行这些操作；它还支持 ZIP 在线更新与复制/ZIP 部署，
即下载文件并落盘到可执行位置。这些路径中的缺陷可能被用于提权或持久化，
因此我们希望相关问题在修复前不要公开。

便携版还有一个安装版没有的特点：**程序目录由用户决定**，可能是可写的、
可能与其他用户共享、也可能位于受保护路径而触发不同的部署行为。涉及目录权限的问题请务必说明具体路径。

## 支持范围

安全修复只针对 `main` 分支和随最新 WindInput 版本分发的启动器。旧版本不做回溯修复。

以下**属于**本仓库的安全问题：

- 提权路径中的缺陷（`-elevate-register` / `-elevate-unregister` 的参数处理、提权自我拉起时的可执行文件解析）
- 在线更新与部署中的问题：下载内容未校验、ZIP 解压路径穿越（Zip Slip）、覆盖写入到预期之外的位置
- 便携目录可被低权限用户写入时导致的 DLL/EXE 劫持
- 与输入法服务通信的命名管道 RPC 中的越权或注入
- 冲突检测被绕过而导致的意外系统状态

以下**不属于**：

- 输入法引擎本身（候选、编码、词库、上屏）的问题——请报告给
  [WindInput 主仓库](https://github.com/huanfeng/WindInput)
- 需要攻击者已经具备管理员权限才能成立的场景
- 用户主动把便携目录放在人人可写的位置所带来的固有风险（但如果启动器**没有**就此给出应有的处理，那属于本仓问题）

## 报告方式

**请不要通过公开的 Issue 报告安全漏洞。**

请使用 GitHub 的私密漏洞报告功能：进入本仓库的
[Security 标签页](../../security/advisories/new)，点击 “Report a vulnerability”。
该渠道只有仓库维护者可见。

报告中请尽量包含：

- 启动器版本、便携目录的完整路径及其权限情况
- 本机是否装有安装版清风输入法（共存与冲突检测会改变行为）
- 复现步骤，以及攻击者需要具备的前提条件（是否需要本地账户、是否需要用户交互）
- `wind_portable -status` 的输出，以及 `<程序目录>\userdata\logs\` 下的相关日志
- 你评估的影响范围

## 处理流程

1. 我们会在收到报告后尽快确认，并在私密通告中与你沟通进展
2. 确认问题后着手修复；由于启动器随 WindInput 的安装包与便携包一起分发，
   修复的发布节奏会与主仓协调
3. 修复发布后公开安全通告；如你愿意，会在通告中致谢

感谢你负责任地披露安全问题。
